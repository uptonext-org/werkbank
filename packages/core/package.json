{
  "name": "@werkbank/core",
  "version": "0.0.0",
  "private": true,
  "description": "Framework-independent domain logic shared by apps/web and apps/mobile.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "oxlint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "oxlint": "^1.79.0",
    "typescript": "^5.7.2",
    "vitest": "^4.1.11"
  }
}
